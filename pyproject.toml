[project]
name = "todo-app"
version = "0.1.0"
description = "A simple todo app with FastAPI and NeonDB"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg2-binary",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"